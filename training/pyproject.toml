[project]
name = "llm-guard-training"
version = "0.1.0"
description = "Training pipeline for llm_guard classifier"
requires-python = ">=3.11"
dependencies = [
  "datasets>=2.21.0",
  "numpy>=1.26.0",
  "scikit-learn>=1.5.0",
]
